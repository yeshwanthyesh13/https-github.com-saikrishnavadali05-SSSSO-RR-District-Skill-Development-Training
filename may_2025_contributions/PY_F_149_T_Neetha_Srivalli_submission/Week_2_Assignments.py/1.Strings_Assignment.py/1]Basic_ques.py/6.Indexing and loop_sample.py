a = "hello"      #Giving a string
for i in range(len(a)):      #Iterating the string using a for loop
    print(s[i])             #Prints each character of the string in a new line
            #s[i] accesses each character using its index
#Output:
# h
# e
# l
# l
# o